apiVersion: v1
kind: Service
metadata:
  name: apphoster-backend-service
spec:
  type: NodePort
  selector:
    app: apphoster-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
