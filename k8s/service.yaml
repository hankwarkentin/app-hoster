apiVersion: v1
kind: Service
metadata:
  name: apphoster-service
spec:
  type: NodePort
  selector:
    app: apphoster
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
